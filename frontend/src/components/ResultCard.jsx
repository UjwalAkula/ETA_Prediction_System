import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faRoute, faClock } from "@fortawesome/free-solid-svg-icons";

const ResultCard = ({ eta }) => {
  if (eta === null) return null;

  return (
    <div className="mt-12 rounded-2xl border border-emerald-200 bg-gradient-to-br from-emerald-50 to-white p-8 text-center shadow-sm">
      
      {/* Icon */}
      <div className="mx-auto mb-4 flex h-12 w-12 items-center justify-center rounded-full bg-emerald-600 text-white shadow-md">
        <FontAwesomeIcon icon={faRoute} className="text-lg" />
      </div>

      <p className="text-xs uppercase tracking-widest text-emerald-700">
        Estimated Delivery Time
      </p>

      <div className="mt-4 flex items-center justify-center gap-3">
        <FontAwesomeIcon
          icon={faClock}
          className="text-2xl text-emerald-700"
        />
        <span className="text-5xl font-semibold text-emerald-900">
          {eta.toFixed(1)}
        </span>
        <span className="text-lg text-emerald-700">min</span>
      </div>

      <p className="mt-5 text-sm text-emerald-800 leading-relaxed">
        ETA estimated using delivery distance, food preparation time,
        traffic conditions, weather, time of day, and courier experience.
      </p>

      <p className="mt-2 text-xs text-emerald-600">
        Prediction generated by a machine learning model trained on
        historical food delivery data
      </p>
    </div>
  );
};

export default ResultCard;
